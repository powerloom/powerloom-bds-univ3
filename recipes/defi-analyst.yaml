name: defi-analyst
description: Periodic pool analytics + random on-chain verification (no-LLM narration).
heartbeat:
  interval_seconds: 300
filters:
  pool_address: "0x88e6A0c2dDD26FEEb64F039a2c41296FcB3f5640"
  project_id: "uniswapv3.eth-usdc-0.05"
client:
  call_timeout_ms: 90000
verification:
  mode: sampled
  sample_probability: 0.2
dispatch:
  channel: stdout
